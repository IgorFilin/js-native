export let sum = (a:number,b:number) => {
    return a + b
}

export let multiplication = (a:number,b:number,c:number) => {
    return a * b * c
}

export let segmentation = (a:number,c:number) => {
    return c / a
}

